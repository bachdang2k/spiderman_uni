# Optional assets

The experience is asset-free by default: illustrations are generated with SVG, Canvas, and CSS. Add optimized local audio, texture, or image files here, then point to them from `src/config/story.ts`.
